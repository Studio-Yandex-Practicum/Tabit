"""
Образец документации для фабрик тестовых данных.

Модуль содержит примеры кода и документации для фабрик,
используемых при тестировании.

Шаблон докстринга для фабрики должен содержать:
   - Описание создаваемого объекта/данных
   - Параметры фабрики с типами
   - Возвращаемое значение с типом
   - Особенности работы (опционально)
"""


def example_factory(param1: int, param2: str = "default") -> dict:
    """
    Пример фабрики для создания тестовых данных.

    Создает словарь с тестовыми данными для использования в тестах.
    Поддерживает кастомизацию через параметры.

    Аргументы:
        param1 (int): Основной параметр, определяющий ключевую характеристику.
        param2 (str, optional): Дополнительный строковый параметр. По умолчанию "default".

    Возвращает:
        dict: Словарь с тестовыми данными, содержащий:
            - ключ 'main_field' на основе param1
            - ключ 'additional_field' на основе param2
            - ключ 'auto_field' с автоматически генерируемым значением

    Особенности:
        - Генерирует уникальные значения при каждом вызове
        - Поддерживает переопределение всех полей
    """


class UserFactory:
    """
    Фабрика для создания тестовых пользователей с расширенными возможностями.

    Позволяет создавать как простых пользователей, так и администраторов,
    поддерживает настройку всех основных атрибутов и генерацию тестовых данных.

    Атрибуты:
        default_role (str): Роль по умолчанию для новых пользователей.
        user_counter (int): Счётчик для генерации уникальных имён.

    Методы:
        create: Создаёт одного пользователя
        create_batch: Создаёт несколько пользователей
        admin: Создаёт пользователя с правами администратора
    """

    def __init__(self, default_role: str = "user"):
        pass

    def create(self, **kwargs) -> dict:
        """
        Создаёт тестового пользователя с возможностью переопределения атрибутов.

        Аргументы:
            **kwargs: Аргументы для переопределения стандартных значений.
                     Поддерживаемые ключи: username, email, role, is_active

        Возвращает:
            dict: Словарь с данными пользователя

        Пример:
            >>> factory = UserFactory()
            >>> user = factory.create(username="custom_name")
        """

    def create_batch(self, size: int) -> list[dict]:
        """
        Создаёт несколько тестовых пользователей.

        Аргументы:
            size (int): Количество пользователей для создания

        Возвращает:
            list[dict]: Список словарей с данными пользователей
        """
