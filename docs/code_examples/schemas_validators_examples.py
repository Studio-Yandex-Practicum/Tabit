"""
Образец реализации для валидаторов схем.

Модуль содержит примеры кода и документации для валидаторов, используемых в Pydantic-схемах.

Шаблон докстринга для валидатора схемы должен содержать:
   - Описание назначения валидатора
   - Параметры функции с типами
   - Возвращаемое значение с типом
   - Возможные исключения
"""


def example_validator(value: str) -> str:
    """
    Пример валидатора для строкового поля.

    Проверяет, что строка соответствует определённым требованиям:
    1. Не пустая
    2. Не состоит только из пробелов
    3. Соответствует минимальной длине

    Аргументы:
        value (str): Строка для валидации.

    Возвращает:
        str: Проверенная и нормализованная строка.

    Исключения:
        ValueError: Возникает, если строка не проходит валидацию.
    """
