"""
Образец реализации для моделей базы данных.

Модуль содержит примеры кода и документации для классов и функций, реализующих модели базы данных.

Шаблон докстринга для модели должен содержать:
   - Описание назначения модели
   - Перечень атрибутов модели с типами
   - Описание связей с другими моделями (опционально)
"""


class ExampleModel:
    """
    Пример модели для работы с базой данных.

    Модель представляет собой маппинг таблицы базы данных на объект Python.
    Содержит описание полей и связей с другими моделями.

    Атрибуты:
        id (UUID): Уникальный идентификатор записи.
        name (str): Название записи.
        created_at (datetime): Дата создания записи.
        updated_at (datetime): Дата последнего обновления записи.

    Связи:
        related_model (RelatedModel): Связь с другой моделью через внешний ключ.
        another_relation (AnotherModel): Связь через ассоциативную таблицу.
    """


class RelatedModel:
    """
    Модель, связанная с ExampleModel через внешний ключ.

    Модель представляет собой связанную сущность, которая используется
    в отношениях с основной моделью ExampleModel.

    Атрибуты:
        id (UUID): Уникальный идентификатор записи.
        example_id (UUID): Внешний ключ, ссылающийся на ExampleModel.
        description (str): Описание связанной записи.
        created_at (datetime): Дата создания записи.

    Связи:
        example (ExampleModel): Обратная связь с основной моделью.
    """


class AnotherModel:
    """
    Модель для связи через ассоциативную таблицу.

    Модель используется для реализации отношения многие-ко-многим
    между ExampleModel и другими сущностями.

    Атрибуты:
        id (UUID): Уникальный идентификатор записи.
        name (str): Название записи.
        examples (List[ExampleModel]): Список связанных ExampleModel.

    Связи:
        example_associations (List[ExampleAssociation]): Связи через ассоциативную таблицу.
    """


class ExampleAssociation:
    """
    Ассоциативная таблица для связи ExampleModel и AnotherModel.

    Модель реализует отношение многие-ко-многим между ExampleModel
    и AnotherModel через промежуточную таблицу.

    Атрибуты:
        example_id (UUID): Внешний ключ, ссылающийся на ExampleModel.
        another_id (UUID): Внешний ключ, ссылающийся на AnotherModel.
        created_at (datetime): Дата создания связи.
    """
